#include <bits/stdc++.h>

using namespace std;
int soma(vector<int> k){
   int u = 0;
   for(int i = 1;i<k.size();i++){
      u += k[i];
   }
   return u;
}
int main(){
   long long t,c,n;

   while(t--){
      cin >> n;
      vector<int>vec(n);
      for(int i = 0;i<n;i++){
         cin >> vec[v];
      }
      int maior = soma(k);
      int l = 0; int r = n-1;
      maior = max(maior,r + vec[r]);
      for(int i = 0;i<n;i++){
         maior = (soma
      }
   }
}
p
2 + 8 - 4 + 7  - 1 = 12
4 + 9 - 9 + 9 - 9 + 9 = 13
5 + 15 - 14 + 1 - 14 + 1 - 1 = -7;
31 - 12 +14 -22 +89 -6 +78 -25 + 91 =  precisa ser 265
5 + 31 - 12 + 25 - 22 + 89 - 6 + 78 -14 + 91 = 265
 
