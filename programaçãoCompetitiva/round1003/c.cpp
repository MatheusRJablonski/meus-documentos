#include <bits/stdc++.h>

using namespace std;
#define int long long
signed main(){
    int t;
    cin >>t;
    while(t--){
        
        int n,m,f;
        cin >> n >> m;
        vector<int>v(n);
        for(int i = 0;i<n;i++)cin >> v[i];
        cin >> f;
        if(n == 1)cout << "yes" << endl;
        else{
           
        }
    }
}