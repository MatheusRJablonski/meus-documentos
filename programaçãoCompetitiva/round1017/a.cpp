#include <bits/stdc++.h>
 
using namespace std;
 
int main(){
    long long n;
    string s,t,u;
    cin >> n;
    while(n--){
        cin >> s >> t >> u;
        cout << s[0] << t[0] << u[0] << endl;
    }
}